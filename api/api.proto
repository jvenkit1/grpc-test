syntax = "proto3";
package api;

message Date{
  string CurrDate = 28;
}

service Greet{
  rpc SayDate(Date) returns (Date) {}
}

/*
We have exposed a service called Greet, that provides a function called sayDate

sayDate returns a Date object.
 */